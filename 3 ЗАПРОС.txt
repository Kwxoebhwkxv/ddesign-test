-- Вывести отдел с максимальной суммарной зарплатой сотрудников.

USE DigitalDesign

SELECT TOP 1
	DEPARTMENT.NAME

FROM DEPARTMENT
JOIN EMPLOYEE ON DEPARTMENT.ID = EMPLOYEE.DEPARTMENT_ID

GROUP BY DEPARTMENT.NAME
ORDER BY SUM(SALARY) DESC